'use client';
import{FormEvent,useState}from'react';
import{useRouter}from'next/navigation';
export default function Account(){const router=useRouter(),[mode,setMode]=useState<'login'|'signup'>('login'),[role,setRole]=useState<'passenger'|'driver'>('passenger'),[busy,setBusy]=useState(false),[msg,setMsg]=useState(''),[f,setF]=useState({name:'',phone:'',email:'',password:''});const set=(k:string,v:string)=>setF({...f,[k]:v});async function send(e:FormEvent){e.preventDefault();setBusy(true);setMsg('');try{const r=await fetch('/api/account',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action:mode,...f,role})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Unable to continue.');router.push(j.role==='driver'?'/driver':'/passenger');router.refresh()}catch(e){setMsg(e instanceof Error?e.message:'Unable to continue.')}finally{setBusy(false)}}return <main className="shell section"><div style={{maxWidth:580,margin:'auto'}}><div className="eyebrow">A2B RIDES ACCOUNT</div><h1 style={{fontSize:'clamp(2.5rem,7vw,4.2rem)',margin:'10px 0'}}>SIGN IN &amp; RIDE</h1><div className="account-tabs"><button className={`btn ${mode==='login'?'btn-gold':'btn-outline'}`} onClick={()=>setMode('login')}>Sign In</button><button className={`btn ${mode==='signup'?'btn-gold':'btn-outline'}`} onClick={()=>setMode('signup')}>Create Account</button></div>{mode==='signup'&&<div className="role-picker"><button className={role==='passenger'?'selected':''} onClick={()=>setRole('passenger')}><strong>Passenger</strong><span>Book and track rides</span></button><button className={role==='driver'?'selected':''} onClick={()=>setRole('driver')}><strong>Approved Driver</strong><span>View assigned trips</span></button></div>}<form className="panel account-card" onSubmit={send}>{mode==='signup'&&<><label>Full name<input required className="input" autoComplete="name" value={f.name} onChange={e=>set('name',e.target.value)}/></label><label>Mobile number<input required className="input" inputMode="tel" autoComplete="tel" value={f.phone} onChange={e=>set('phone',e.target.value)}/></label></>}<label>Email<input required className="input" type="email" autoComplete="email" value={f.email} onChange={e=>set('email',e.target.value)}/></label><label>Password<input required className="input" type="password" minLength={8} autoComplete={mode==='login'?'current-password':'new-password'} value={f.password} onChange={e=>set('password',e.target.value)}/></label>{mode==='signup'&&role==='driver'&&<p className="notice">Use the same email as your approved A2B driver application.</p>}<button className="btn btn-gold" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign In':'Create Account'}</button>{msg&&<p className="notice error" role="status">{msg}</p>}</form></div></main>}
