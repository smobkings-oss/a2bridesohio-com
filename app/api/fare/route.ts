import{NextResponse}from'next/server';import{priceTrip}from'../../../lib/pricing';import{routeMetrics}from'../../../lib/routes';export async function POST(req:Request){try{const{pickup,dropoff,stops,airport,date,time}=await req.json();const m=await routeMetrics(String(pickup||''),String(dropoff||''),Array.isArray(stops)?stops:[]),q=priceTrip(m.distanceMiles,m.durationMinutes,!!airport,String(date||''),String(time||''));return NextResponse.json({...m,...q,total:q.totalCents/100,subtotal:q.subtotalCents/100,surcharge:q.surchargeCents/100})}catch(e){const s=e instanceof Error?e.message:'';return NextResponse.json({error:s||'Unable to calculate fare.'},{status:s.includes('configured')?503:422})}}
