import{NextResponse}from'next/server';import{randomBytes}from'crypto';import{db}from'../../../lib/db';import{isAdmin}from'../../../lib/auth';import{routeMetrics}from'../../../lib/routes';import{priceTrip}from'../../../lib/pricing';export async function POST(req:Request){try{const x=await req.json();for(const k of['name','phone','pickup','dropoff','date','time'])if(!x[k])return NextResponse.json({error:`${k} is required`},{status:400});const m=await routeMetrics(String(x.pickup),String(x.dropoff)),fare=priceTrip(m.distanceMiles,m.durationMinutes,!!x.airport),publicToken=randomBytes(24).toString('hex');const row={name:String(x.name).slice(0,120),phone:String(x.phone).slice(0,40),pickup:String(x.pickup).slice(0,300),dropoff:String(x.dropoff).slice(0,300),ride_date:x.date,ride_time:x.time,passengers:Math.max(1,Math.min(6,Number(x.passengers||1))),airport:!!x.airport,notes:String(x.notes||'').slice(0,1500),fare_estimate_cents:fare,distance_miles:m.distanceMiles,duration_minutes:m.durationMinutes,status:'New',fare_locked:false,payment_status:'unpaid',public_token:publicToken};const{data,error}=await db().from('ride_requests').insert(row).select('id,public_token,fare_estimate_cents').single();if(error)throw error;return NextResponse.json({id:data.id,publicToken:data.public_token,fareEstimateCents:data.fare_estimate_cents},{status:201})}catch(e:any){return NextResponse.json({error:e.message||'Unable to save request'},{status:500})}}export async function PATCH(req:Request){if(!await isAdmin())return NextResponse.json({error:'Unauthorized'},{status:401});try{const x=await req.json();if(!x.id)return NextResponse.json({error:'id required'},{status:400});const patch:any={updated_at:new Date().toISOString()};if(x.status)patch.status=x.status;if(typeof x.fareLocked==='boolean')patch.fare_locked=x.fareLocked;if(Number.isInteger(x.lockedFareCents)&&x.lockedFareCents>0)patch.locked_fare_cents=x.lockedFareCents;const{error}=await db().from('ride_requests').update(patch).eq('id',x.id);if(error)throw error;return NextResponse.json({ok:true})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}