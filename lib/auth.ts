import{cookies}from'next/headers';import{createHmac,timingSafeEqual}from'crypto';const NAME='a2b_admin';function sig(){const secret=process.env.ADMIN_SESSION_SECRET;if(!secret)throw new Error('ADMIN_SESSION_SECRET missing');return createHmac('sha256',secret).update('a2b-admin').digest('hex')}export async function isAdmin(){const c=(await cookies()).get(NAME)?.value;if(!c)return false;const a=Buffer.from(c),b=Buffer.from(sig());return a.length===b.length&&timingSafeEqual(a,b)}export function adminCookieValue(){return sig()}export const adminCookieName=NAME;