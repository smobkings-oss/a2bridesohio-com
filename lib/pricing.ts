export function priceTrip(miles:number,minutes:number,airport=false){const base=Number(process.env.FARE_BASE||3.5),pm=Number(process.env.FARE_PER_MILE||1.65),pmin=Number(process.env.FARE_PER_MINUTE||.25),minimum=Number(process.env.FARE_MINIMUM||12),airportFee=airport?Number(process.env.FARE_AIRPORT_FEE||10):0;const total=Math.max(minimum,base+miles*pm+minutes*pmin+airportFee);return Math.round(total*100)}